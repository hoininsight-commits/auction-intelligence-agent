# Progress Log (append-only)

> 세션 종료 시 아래 형식으로 요약을 추가한다. 과거 대화 대신 이 파일을 신뢰한다.

## 형식
- [YYYY-MM-DD] Phase N: 한 일 / 다음 할 일 / 열린 이슈

## 로그
- [2026-07-10] Phase 1~6: MVP 전체 구현 완료(서브에이전트 병렬 실행). pytest 31개 통과. GitHub 저장소(`hoininsight-commits/auction-intelligence-agent`) 생성 및 push. Docker Desktop 설치 후 `docker compose up`으로 실기동 검증 완료.
- [2026-07-10] 후순위 기능: 실제 온비드 API, 실제 국토부 실거래가 API, 관리자 웹(`/admin`) 구현 및 커밋/푸시 완료. ML모델/푸시알림/결제/모바일앱은 스코프 미정으로 스킵 결정.
- [2026-07-10] 버그 수정: pytest가 `conftest.py`의 `setdefault` 때문에 Docker 환경에서 dev DB(`auction_db`)를 DROP/TRUNCATE하던 문제 발견 및 수정 — `DATABASE_URL`을 항상 `TEST_DATABASE_URL`로 강제 override하도록 변경.
- [2026-07-10] Claude Code 설정 세트(`.claude/memory`, `.claude/commands`, `.claude/agents`) 도입, CLAUDE.md를 진입점 형태로 슬림화.
- [2026-07-10] 개인정보 마스킹 구현: `app/core/masking.py`(전화번호/주민등록번호/차량번호/라벨링된 이름 정규식 마스킹), `raw_source_repository.py`에 적용, 실 Postgres에 마스킹된 값으로 저장되는 것 확인. ruff 도입 및 미사용 import 6건 자동 정리(pytest 37개 전체 통과 유지). 남은 이슈: 모델 파일 4곳의 SQLAlchemy 문자열 forward-ref에 대한 ruff F821 10건(런타임 정상 동작, `TYPE_CHECKING` 가드 미비로 인한 lint 오탐 — 다음 세션 후보).
- [2026-07-11] 국토부 커넥터 법정동코드 매핑(`real_transaction_connector.py`의 `_LAWD_CD_MAP`) 확장: 기존 9개 시군구 → 전국 17개 시도 250개 시/군/구 전체(2023년 대구 군위군 편입 등 최신 개편 반영, 출처 code.go.kr). 세종특별자치시처럼 구/군 구분 없는 지역을 위해 `_resolve_lawd_cd`에 sido 단독 매핑 조회 분기 추가, 에러 메시지가 250개 전체를 나열하지 않도록 개수 요약으로 축약. 이 세션(집)엔 docker/pytest/ruff가 설치돼 있지 않아 `/run-checks` 미실행 — 문법 검증(ast.parse)과 매핑 중복 키/코드 검사만 통과 확인. 다음 세션에서 Docker 환경으로 `/run-checks` 재검증 필요.
- [2026-07-11] ruff F821 10건 해소: `app/models/{auction_item,auction_result,real_estate_detail,vehicle_detail,price_prediction,risk_assessment}.py` 6개 파일에 `TYPE_CHECKING` 가드로 서로의 클래스를 import해 forward-ref(`Mapped["AuctionItem"]` 등)를 정적 분석 가능하게 만듦. TYPE_CHECKING 블록이라 런타임 실행 안 되므로 순환 import 위험 없음. 이 세션도 Docker/ruff가 없어 `ruff check` 0건 확인은 못 함 — 문법 검증만 통과.
- [2026-07-11] 집 Mac(Mac mini, M4)에 Homebrew + Docker Desktop 신규 설치. 첫 실행 시 VM 엔진이 계속 "starting"에서 멈추는 문제 발생 — 원인은 이전 설치 실패 시도(심볼릭 링크 단계 sudo 실패로 롤백)가 남긴 `~/Library/Containers/com.docker.docker/Data` 잔여 상태로 추정. 해당 디렉터리 초기화(`rm -rf`, 다운로드된 이미지 없어 안전) 후 재실행하니 정상 기동. `docker compose up -d --build` + `alembic upgrade head` + `seed_sample_data` 성공. `/run-checks` 실행: `ruff check` 전체 통과(F821 0건, 국토부 매핑 확장/모델 forward-ref 수정 모두 검증됨), `alembic upgrade head` 정상, pytest는 `auction_test_db`가 없어서 최초 37건 전부 `InvalidCatalogNameError` — `CREATE DATABASE auction_test_db` 수동 생성 후 37/37 통과. `/docs`(200)·`/admin`(307 redirect)·`/health` 정상 응답. README에 테스트 DB 최초 생성 단계 누락 발견해 추가.
- [2026-07-11] `ruff format .` 일괄 적용: 지금까지 lint만 돌리고 format은 실행한 적이 없어 쌓인 75개 파일 스타일 드리프트(모듈 docstring 뒤 빈 줄, 88자 기준 줄 재배치 등) 해소. 로직 변경 없음 — `ruff check` 전체 통과, pytest 37/37 재확인 완료.
- [2026-07-11] 온비드/국토부 실제 API 실호출 검증: data.go.kr 활용신청 → 서비스키 발급 후 국토부 실거래가(`getRTMSDataSvcAptTradeDev`)는 기존 구현 그대로 정상 응답 확인(Base URL만 http→https 수정). 온비드는 기존 코드가 참조하던 데이터셋(`15000851`, `getKamcoPbctCltrList`)이 폐기(404)된 것을 발견 — 실제로는 "차세대 온비드" API 2종(물건목록 `OnbidRlstListSrvc2/getRlstCltrList2`, 물건상세 `OnbidCltrBidDtlSrvc2/getCltrBidInf2`)으로 이전됨을 확인하고 `onbid_connector.py` 전면 재작성. 필수 파라미터(`prptDivCd`, `pvctTrgtYn`, `cltrMngNo`, `pbctCdtnNo`)는 data.go.kr에서 다운로드한 공식 기술문서(docx)를 직접 파싱해서 확정. `source_item_id` 포맷이 `{cltrMngNo}-{pbctCdtnNo}`에서 `{cltrMngNo}::{pbctCdtnNo}`로 변경(cltrMngNo 자체에 하이픈 포함). 실제 서비스키로 물건목록/물건상세 모두 실호출 성공 확인. `.env.example` 갱신, `/run-checks` 재검증(ruff check/format/pytest 37/37) 통과.
- [2026-07-11] `ENABLE_MOCK_CONNECTORS=false`로 전환 후 실제 데이터 수집 파이프라인(`collect_source_items`) end-to-end 테스트. 온비드는 파라미터 없이 100건 수집/삽입 성공. 국토부는 `LAWD_CD`가 필수라 `collect_source_items(source)`가 파라미터를 못 넘기는 기존 구조에서 즉시 실패(앱 크래시 없이 `collection_jobs.status="failed"`로는 정상 기록됨)하는 것을 확인 — `collect_source_items(source, **connector_kwargs)`로 확장해 `fetch_items(**connector_kwargs)`에 그대로 전달하도록 수정, `app/workers/tasks.py`의 Celery task와 `app/ingestion/scheduler.py`의 `ScheduledJob`에 `default_kwargs` 필드를 추가해 관통시켰다(`real_transaction` 스케줄 항목은 예시로 서울 종로구를 기본값으로 채움 — 전국 250개 시/군/구 순회 로직은 아직 없어 후속 작업 후보로 남김). 재검증: 온비드(100건)/국토부(서울 종로구 20건→19신규+1갱신) 둘 다 raw 저장→정규화 upsert→`collection_jobs` 로그까지 성공, 재실행 시 upsert 멱등성(같은 데이터가 `updated_count`로 집계)도 확인. `/run-checks` 최종 재확인(ruff check/format/pytest 37/37) 통과. `.env`는 이제 `ENABLE_MOCK_CONNECTORS=false` 상태로 남아있음(이 Mac 한정, 실제 API를 계속 호출하니 트래픽 한도 유의).
