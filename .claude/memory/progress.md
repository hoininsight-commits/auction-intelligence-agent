# Progress Log (append-only)

> 세션 종료 시 아래 형식으로 요약을 추가한다. 과거 대화 대신 이 파일을 신뢰한다.

## 형식
- [YYYY-MM-DD] Phase N: 한 일 / 다음 할 일 / 열린 이슈

## 로그
- [2026-07-10] Phase 1~6: MVP 전체 구현 완료(서브에이전트 병렬 실행). pytest 31개 통과. GitHub 저장소(`hoininsight-commits/auction-intelligence-agent`) 생성 및 push. Docker Desktop 설치 후 `docker compose up`으로 실기동 검증 완료.
- [2026-07-10] 후순위 기능: 실제 온비드 API, 실제 국토부 실거래가 API, 관리자 웹(`/admin`) 구현 및 커밋/푸시 완료. ML모델/푸시알림/결제/모바일앱은 스코프 미정으로 스킵 결정.
- [2026-07-10] 버그 수정: pytest가 `conftest.py`의 `setdefault` 때문에 Docker 환경에서 dev DB(`auction_db`)를 DROP/TRUNCATE하던 문제 발견 및 수정 — `DATABASE_URL`을 항상 `TEST_DATABASE_URL`로 강제 override하도록 변경.
- [2026-07-10] Claude Code 설정 세트(`.claude/memory`, `.claude/commands`, `.claude/agents`) 도입, CLAUDE.md를 진입점 형태로 슬림화.
- [2026-07-10] 개인정보 마스킹 구현: `app/core/masking.py`(전화번호/주민등록번호/차량번호/라벨링된 이름 정규식 마스킹), `raw_source_repository.py`에 적용, 실 Postgres에 마스킹된 값으로 저장되는 것 확인. ruff 도입 및 미사용 import 6건 자동 정리(pytest 37개 전체 통과 유지). 남은 이슈: 모델 파일 4곳의 SQLAlchemy 문자열 forward-ref에 대한 ruff F821 10건(런타임 정상 동작, `TYPE_CHECKING` 가드 미비로 인한 lint 오탐 — 다음 세션 후보).
- [2026-07-11] 국토부 커넥터 법정동코드 매핑(`real_transaction_connector.py`의 `_LAWD_CD_MAP`) 확장: 기존 9개 시군구 → 전국 17개 시도 250개 시/군/구 전체(2023년 대구 군위군 편입 등 최신 개편 반영, 출처 code.go.kr). 세종특별자치시처럼 구/군 구분 없는 지역을 위해 `_resolve_lawd_cd`에 sido 단독 매핑 조회 분기 추가, 에러 메시지가 250개 전체를 나열하지 않도록 개수 요약으로 축약. 이 세션(집)엔 docker/pytest/ruff가 설치돼 있지 않아 `/run-checks` 미실행 — 문법 검증(ast.parse)과 매핑 중복 키/코드 검사만 통과 확인. 다음 세션에서 Docker 환경으로 `/run-checks` 재검증 필요.
- [2026-07-11] ruff F821 10건 해소: `app/models/{auction_item,auction_result,real_estate_detail,vehicle_detail,price_prediction,risk_assessment}.py` 6개 파일에 `TYPE_CHECKING` 가드로 서로의 클래스를 import해 forward-ref(`Mapped["AuctionItem"]` 등)를 정적 분석 가능하게 만듦. TYPE_CHECKING 블록이라 런타임 실행 안 되므로 순환 import 위험 없음. 이 세션도 Docker/ruff가 없어 `ruff check` 0건 확인은 못 함 — 문법 검증만 통과.
- [2026-07-11] 경쟁사 리서치(deep-research) 진행: 국내 유료 3강(지지옥션/옥션원/굿옥션)·공적 무료 채널(대법원경매정보/온비드)·신흥 AI 권리분석 서비스(요이땅/해설집/땅집고옥션 등)·해외 압류경매(Auction.com/RealtyTrac/Hubzu/Xome/GovDeals)를 조사해 우리 기능과 대조한 리포트를 Artifact로 발행. 프론트엔드 디자인 핸드오프용으로 `docs/frontend-handoff-물건카드.md` 작성(검색/상세 API 필드 스펙 + 아직 없는 view_count·watch_count 노출/종합판정 배지 설계).
- [2026-07-11] `view_count`/`watch_count` 실제 증가 로직 구현: `auction_item_repository.increment_view_count`(UPDATE...RETURNING, 상세 조회 시 호출)와 `adjust_watch_count`(watchlist 추가/삭제 시 ±1, `func.greatest(...,0)`로 음수 방지)를 추가. `AuctionItemSearchItem`/`AuctionItemDetailResponse` 스키마와 검색/상세 서비스에 두 필드 노출. 실제 Docker 서버에 curl로 상세 2회 호출(0→1→2), 관심물건 추가/삭제(0→1→0) 검증 완료. `/run-checks` 전체(ruff check/format, alembic, pytest 37/37, /docs) 통과.
- [2026-07-11] 종합판정 배지(`verdict`: `good`/`fair`/`caution`) 구현: 새 계산식·새 데이터 없이 rule-v1 내부에서 이미 계산하던 시세괴리율(`_compute_price_gap_coefficient`가 이제 `(coefficient, ratio)` 튜플 반환)과 `risk_level`을 조합하는 룰 하나(`_determine_verdict`)만 추가. 임계값은 기존 §8.7 시세괴리 구간 상수(`PRICE_GAP_COEFFICIENT_BANDS`)를 그대로 재사용(`VERDICT_GOOD_PRICE_GAP_MAX`/`VERDICT_CAUTION_PRICE_GAP_MIN`). DB 마이그레이션 없이 기존 `price_predictions.explanation` JSONB에 `verdict` 키로 저장하고, 검색/상세 API가 이를 읽어 노출. `predict-price` API로 실제 호출해 `risk_level=high`인 물건이 `verdict=caution`으로 정확히 나오는 것 확인. `risk_level="low"`는 기본 위험도 로직에서 사실상 안 나와서(정보부족=unknown으로 보수적 처리) `verdict=good`이 드물게만 나오는데, 의도된 보수적 설계(증거 없이 "우수" 주지 않음). `docs/frontend-handoff-물건카드.md`에 API 값→화면 라벨 매핑표 갱신. `/run-checks` 전체 통과.
- [2026-07-11] 사용자가 법원경매 크롤링을 요청 → 절대 규칙(개인정보보호법/정보통신망법/부정경쟁방지법/업무방해 리스크) 근거로 명확히 거절, CLAUDE.md 규칙 삭제 요청도 거절. 대신 "실제 데이터를 더 많이 보여준다"는 목표를 합법적 경로(기존 공식 API 활용도 개선, 유료업체 제휴 조사)로 재조준하기로 함.
- [2026-07-11] 온비드 페이지네이션 버그 발견 및 수정: 실제 API 질의 결과 전체 매물이 64,440건인데 기존 파이프라인은 `pageNo=1` 하드코딩으로 매시간 항상 같은 100건만 수집하고 있었음(`app/ingestion/onbid_connector.py`). `fetch_items`가 `numOfRows`씩 여러 페이지를 순회하도록 수정(기본 `max_pages=20` → 최대 2,000건/호출), 짧은 페이지·빈 페이지를 만나면 자동 중단. 실 파이프라인으로 검증: `auction_items` 온비드 건수 100건 → 2,002건으로 증가(1회 수집에 1,900건 신규 삽입). `ponytail` 한계 주석 명시: 매 호출이 항상 1페이지부터 시작해 같은 구간만 반복 조회(회전 커서 없음) — 업그레이드하려면 마지막 페이지를 Redis 등에 저장해 이어서 시작하게 만들 것.
- [2026-07-11] 국토부 실거래가 매물종별 확장(아파트 외 4종): `real_transaction_connector.py`를 `PROPERTY_TYPE_CONFIG` 딕셔너리 기반으로 일반화해 오피스텔(`RTMSDataSvcOffiTrade`)/연립다세대(`RTMSDataSvcRHTrade`)/단독·다가구(`RTMSDataSvcSHTrade`)/상업업무용(`RTMSDataSvcNrgTrade`) API를 `property_type` kwarg로 선택 가능하게 만듦(엔드포인트/필드명은 WooilJeong/PublicDataReader 레퍼런스 구현 기준). `collect_real_transaction_all_regions_task`에 `property_type` 파라미터 추가(미지정 시 기존과 동일하게 apartment만, 하위호환). 실제 서비스키로 4종 모두 호출해봤으나 전부 **403 Forbidden** — data.go.kr에서 아파트 데이터셋만 활용신청 승인되어 있고 나머지 4개는 미승인 상태. 코드는 완성됐고 안전하게 실패 처리됨(`collection_jobs.status=failed`) — **다음 세션 전 data.go.kr 마이페이지에서 4개 데이터셋 활용신청 필요**, 승인 확인되면 beat_schedule에 항목 추가만 하면 자동 수집 가능. `tests/test_real_data_connectors.py` 신규(온비드 페이지네이션 3건 + MOLIT 매물종별 정규화 6건). `/run-checks` 전체 통과(46/46).
- [2026-07-12] 사용자가 국토부 매물종별 4종 활용신청 완료했다고 알렸으나 재확인 결과 여전히 403(순수 텍스트 "Forbidden", XML 에러 아님) — data.go.kr 승인 반영 지연으로 판단, 30분 후 자동 재확인 예약(ScheduleWakeup).
- [2026-07-12] 관리 페이지(`/admin`) 리스트 UX 버그 3건 수정: (1) `sido`/`sigungu`/`eupmyeondong` 검색이 완전일치(`==`)라 "서울" 검색 시 DB의 "서울특별시"와 매치 안 되던 문제 → `ilike` 부분일치로 변경(`auction_item_repository.py`). (2) 부동산/차량을 구분해 볼 UI가 없던 문제 → `auction_type` 필터에 콤마 구분 다중값(IN절) 지원 추가하고 관리 페이지에 전체/부동산(`real_estate,public_sale`)/차량 탭 UI 추가. (3) 정렬 기준을 고를 UI가 없어 항상 입찰일순 고정이던 문제 → 기존 API가 이미 지원하던 8개 정렬 옵션(`bid_date_asc/desc`, `minimum_price_asc/desc`, `appraisal_price_asc/desc`, `fail_count_desc`, `created_at_desc`)을 드롭다운으로 노출. 부수적으로 카테고리 드롭다운에 `house`(171건)/`unknown`(174건)이 누락되어 345건이 필터 불가능했던 것도 발견해 옵션 추가. 실서버 curl로 서울 검색(85건)/부동산 탭(2,006건)/차량 탭(1건)/정렬 전부 검증. `/run-checks` 전체 통과(46/46).
- [2026-07-11] 집 Mac(Mac mini, M4)에 Homebrew + Docker Desktop 신규 설치. 첫 실행 시 VM 엔진이 계속 "starting"에서 멈추는 문제 발생 — 원인은 이전 설치 실패 시도(심볼릭 링크 단계 sudo 실패로 롤백)가 남긴 `~/Library/Containers/com.docker.docker/Data` 잔여 상태로 추정. 해당 디렉터리 초기화(`rm -rf`, 다운로드된 이미지 없어 안전) 후 재실행하니 정상 기동. `docker compose up -d --build` + `alembic upgrade head` + `seed_sample_data` 성공. `/run-checks` 실행: `ruff check` 전체 통과(F821 0건, 국토부 매핑 확장/모델 forward-ref 수정 모두 검증됨), `alembic upgrade head` 정상, pytest는 `auction_test_db`가 없어서 최초 37건 전부 `InvalidCatalogNameError` — `CREATE DATABASE auction_test_db` 수동 생성 후 37/37 통과. `/docs`(200)·`/admin`(307 redirect)·`/health` 정상 응답. README에 테스트 DB 최초 생성 단계 누락 발견해 추가.
- [2026-07-11] `ruff format .` 일괄 적용: 지금까지 lint만 돌리고 format은 실행한 적이 없어 쌓인 75개 파일 스타일 드리프트(모듈 docstring 뒤 빈 줄, 88자 기준 줄 재배치 등) 해소. 로직 변경 없음 — `ruff check` 전체 통과, pytest 37/37 재확인 완료.
- [2026-07-11] 온비드/국토부 실제 API 실호출 검증: data.go.kr 활용신청 → 서비스키 발급 후 국토부 실거래가(`getRTMSDataSvcAptTradeDev`)는 기존 구현 그대로 정상 응답 확인(Base URL만 http→https 수정). 온비드는 기존 코드가 참조하던 데이터셋(`15000851`, `getKamcoPbctCltrList`)이 폐기(404)된 것을 발견 — 실제로는 "차세대 온비드" API 2종(물건목록 `OnbidRlstListSrvc2/getRlstCltrList2`, 물건상세 `OnbidCltrBidDtlSrvc2/getCltrBidInf2`)으로 이전됨을 확인하고 `onbid_connector.py` 전면 재작성. 필수 파라미터(`prptDivCd`, `pvctTrgtYn`, `cltrMngNo`, `pbctCdtnNo`)는 data.go.kr에서 다운로드한 공식 기술문서(docx)를 직접 파싱해서 확정. `source_item_id` 포맷이 `{cltrMngNo}-{pbctCdtnNo}`에서 `{cltrMngNo}::{pbctCdtnNo}`로 변경(cltrMngNo 자체에 하이픈 포함). 실제 서비스키로 물건목록/물건상세 모두 실호출 성공 확인. `.env.example` 갱신, `/run-checks` 재검증(ruff check/format/pytest 37/37) 통과.
- [2026-07-11] `ENABLE_MOCK_CONNECTORS=false`로 전환 후 실제 데이터 수집 파이프라인(`collect_source_items`) end-to-end 테스트. 온비드는 파라미터 없이 100건 수집/삽입 성공. 국토부는 `LAWD_CD`가 필수라 `collect_source_items(source)`가 파라미터를 못 넘기는 기존 구조에서 즉시 실패(앱 크래시 없이 `collection_jobs.status="failed"`로는 정상 기록됨)하는 것을 확인 — `collect_source_items(source, **connector_kwargs)`로 확장해 `fetch_items(**connector_kwargs)`에 그대로 전달하도록 수정, `app/workers/tasks.py`의 Celery task와 `app/ingestion/scheduler.py`의 `ScheduledJob`에 `default_kwargs` 필드를 추가해 관통시켰다(`real_transaction` 스케줄 항목은 예시로 서울 종로구를 기본값으로 채움 — 전국 250개 시/군/구 순회 로직은 아직 없어 후속 작업 후보로 남김). 재검증: 온비드(100건)/국토부(서울 종로구 20건→19신규+1갱신) 둘 다 raw 저장→정규화 upsert→`collection_jobs` 로그까지 성공, 재실행 시 upsert 멱등성(같은 데이터가 `updated_count`로 집계)도 확인. `/run-checks` 최종 재확인(ruff check/format/pytest 37/37) 통과. `.env`는 이제 `ENABLE_MOCK_CONNECTORS=false` 상태로 남아있음(이 Mac 한정, 실제 API를 계속 호출하니 트래픽 한도 유의).
- [2026-07-11] cron/스케줄러(Celery Beat) 실제 연결 + 국토부 전국 순회 구현. `real_transaction_connector.py`에 `get_supported_regions()` 추가(`_LAWD_CD_MAP` 250개 시/군/구를 (sido, sigungu) 튜플로 반환, 세종처럼 구/군 없는 지역은 sigungu=""). `app/workers/tasks.py`에 `collect_real_transaction_all_regions_task`(전국 250개 지역을 순회하며 지역당 별도 `collect_source_items` 호출, 한 지역 예외가 전체를 막지 않도록 방어적 try/except) 추가. `app/workers/celery_app.py`에 `beat_schedule` 추가(온비드 매시간, 국토부 실거래가 전국 순회 매일 새벽 3시 KST). `docker-compose.yml`에 `worker`(`celery worker`)/`beat`(`celery beat`) 서비스 추가, 둘 다 정상 기동 확인. 태스크 큐잉(`.delay()`)→워커 실행→결과 반환 전체 경로를 직접 검증했고, 국토부 3개 지역(종로구/중구/세종특별자치시) 실호출 성공도 확인. `/run-checks` 재확인(ruff check/format/pytest 37/37) 통과. README에 worker/beat 안내 추가.
- [2026-07-11] beat 실제 발화 로그 확인 및 버그 수정. beat을 9시간 실행한 뒤 로그 확인 — 온비드 매시간 트리거는 8회 정상 발화했지만, 그중 정확히 절반(홀수 회차)이 `RuntimeError: ... attached to a different loop`로 실패하는 것을 발견. 원인: Celery worker가 매 태스크마다 `asyncio.run()`으로 새 이벤트 루프를 생성하는데, `app/core/database.py`의 전역 `AsyncEngine` 커넥션 풀은 프로세스 전역으로 재사용되어 이전(이미 닫힌) 루프에 묶인 커넥션을 다음 실행에서 재사용하려다 충돌했다. `tasks.py`의 각 Celery task 실행 끝에 `engine.dispose()`하는 `_run_and_dispose_engine()` 헬퍼로 감싸서 해결(FastAPI `app` 컨테이너는 별도 프로세스라 영향 없음). 수정 후 동일 태스크 4회 연속 실행 전부 성공 확인. 국토부 전국 순회(daily crontab)는 beat 시작 시각이 그날 3시 이후라 다음날 03:00 KST로 예약된 것을 `celerybeat-schedule` shelve 파일의 `last_run_at`으로 확인 — 이건 버그가 아니라 정상 동작. `/run-checks` 재확인(ruff check/format/pytest 37/37) 통과. 이것으로 HANDOFF.md의 "알려진 이슈" 항목이 전부 해소됨(§7.7 지시서 예시 오류만 재논의 불필요한 확정 사항으로 남음).
